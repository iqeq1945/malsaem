# 향후 개발 계획

우리말샘 MCP 서버의 향후 개선 사항입니다.

## 🚀 주요 개선 사항

### 1. 라우터 라이브러리 도입

**현재 상태**: if-else 체인으로 경로 처리  
**개선 방안**: Hono 또는 Itty Router 라이브러리 도입

**예상 효과**:

- 코드 가독성 향상
- 경로 추가 용이
- 미들웨어 지원
- 타입 안전성 향상

**작업 내용**:

```typescript
// Hono 도입 예시
import { Hono } from "hono";

const app = new Hono<{ Bindings: Env }>();
app.get("/", handleRoot);
app.get("/search", handleSearch);
app.get("/health", handleHealth);
app.onError(handleError);
```

**예상 작업 시간**: 1-2시간

---

### 2. Health Check 엔드포인트 및 모니터링 봇

**현재 상태**: 루트 경로(`/`)를 Health Check로 사용  
**개선 방안**: 전용 `/health` 엔드포인트 추가 및 모니터링 봇 연동

**작업 내용**:

- `/health` 엔드포인트 구현
  - API 키 설정 상태 확인
  - 서비스 상태 정보 제공
  - 간단한 API 동작 테스트 (선택사항)
- BetterStack/UptimeRobot 등 모니터링 서비스 연동
  - 자동 Health Check 설정
  - 다운타임 알림 설정
  - 성능 모니터링

**Health Check 응답 예시**:

```json
{
  "status": "healthy",
  "timestamp": "2024-12-25T12:00:00.000Z",
  "service": "우리말샘 MCP 서버",
  "version": "1.0.0",
  "apiKeyConfigured": true,
  "apiWorking": true
}
```

**예상 작업 시간**: 1-2시간

---

## 📋 구현 순서

### Phase 1: Health Check 개선

1. `/health` 엔드포인트 추가
2. BetterStack 모니터 설정
3. 알림 테스트

### Phase 2: 라우터 도입

1. Hono 라이브러리 설치
2. 기존 코드를 Hono로 마이그레이션
3. 테스트 및 검증

---

## 📌 참고사항

- 모든 개선 사항은 기존 기능을 깨뜨리지 않는 범위에서 진행
- 각 기능 추가 전에 테스트 권장
- 문서화는 기능 개발과 동시에 진행

---

**마지막 업데이트**: 2024-12-25
